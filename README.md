# konsu.dev

Personal site of Konstantin Suvorov, built with [Astro](https://astro.build).

- `npm install` then `npm run dev` to work locally
- Posts live in `src/content/blog/*.md` (frontmatter: title, description, date)
- Pushing the `astro` branch deploys to GitHub Pages (see `.github/workflows/deploy.yaml`)
